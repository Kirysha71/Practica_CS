<Window x:Class="GradeJournal.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Электронный журнал" Height="700" Width="1300" WindowState="Maximized">

    <Window.Resources>
        <Storyboard x:Key="FadeInStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5"/>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" 
                           From="20" To="0" Duration="0:0:0.5"/>
        </Storyboard>
    </Window.Resources>

    <Window.InputBindings>
        <KeyBinding Gesture="Ctrl+S" Command="{Binding SaveCommand}"/>
        <KeyBinding Gesture="Ctrl+N" Command="{Binding AddCommand}"/>
        <KeyBinding Gesture="F2" Command="{Binding EditCommand}"/>
        <KeyBinding Gesture="Del" Command="{Binding DeleteCommand}"/>
    </Window.InputBindings>

    <Grid>
        <Rectangle Fill="White" Opacity="0.9" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" Panel.ZIndex="10"/>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" Panel.ZIndex="11">
            <TextBlock Text="Загрузка данных..." FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            <ProgressBar Width="400" Height="25" Value="{Binding Progress}" Margin="0,10"/>
            <TextBlock Text="{Binding Progress, StringFormat={}{0:F0}%}" FontSize="16" HorizontalAlignment="Center" Margin="0,10"/>
        </StackPanel>

        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Menu Grid.Row="0" FontSize="14">
                <MenuItem Header="Файл">
                    <MenuItem Header="Загрузить" Command="{Binding LoadCommand}"/>
                    <MenuItem Header="Сохранить" Command="{Binding SaveCommand}"/>
                    <Separator/>
                    <MenuItem Header="Выход"/>
                </MenuItem>
                <MenuItem Header="Студенты">
                    <MenuItem Header="Добавить" Command="{Binding AddCommand}"/>
                    <MenuItem Header="Редактировать" Command="{Binding EditCommand}"/>
                    <MenuItem Header="Удалить" Command="{Binding DeleteCommand}"/>
                </MenuItem>
                <MenuItem Header="Чат">
                    <MenuItem Header="Открыть чат" Click="OpenChat_Click" InputGestureText="Ctrl+T"/>
                </MenuItem>
            </Menu>

            <ToolBarTray Grid.Row="1" Margin="0,10,0,10">
                <ToolBar>
                    <Button Content="📥 Загрузить" Command="{Binding LoadCommand}" Padding="10,5" Margin="2"/>
                    <Button Content="💾 Сохранить" Command="{Binding SaveCommand}" Padding="10,5" Margin="2"/>
                    <Separator/>
                    <Button Content="➕ Добавить" Command="{Binding AddCommand}" Padding="10,5" Margin="2"/>
                    <Button Content="✏️ Редактировать" Command="{Binding EditCommand}" Padding="10,5" Margin="2" 
                            Background="#E3F2FD"/>
                    <Button Content="🗑️ Удалить" Command="{Binding DeleteCommand}" Padding="10,5" Margin="2" Background="#FFCCCC"/>
                    <Separator/>
                    <Button Content="💬 Чат" Click="OpenChat_Click" Padding="10,5" Margin="2" 
                            Background="#E8F5E9" ToolTip="Открыть чат (Ctrl+T)"/>
                </ToolBar>
            </ToolBarTray>

            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Всего студентов: " FontWeight="Bold" FontSize="14"/>
                <TextBlock Text="{Binding Students.Count}" FontSize="14" Margin="0,0,30,0"/>
                <TextBlock Text="Средний балл: " FontWeight="Bold" FontSize="14"/>
                <TextBlock Text="{Binding GroupAverage, StringFormat={}{0:F2}}" FontSize="14" Foreground="Blue" FontWeight="Bold"/>
            </StackPanel>

            <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Students}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderThickness="2" Margin="5" Padding="20" CornerRadius="8" Background="White"
                                    MouseLeftButtonDown="StudentCard_MouseLeftButtonDown"
                                    Cursor="Hand"
                                    Tag="{Binding}">
                                <Border.RenderTransform>
                                    <TranslateTransform/>
                                </Border.RenderTransform>
                                <Border.Triggers>
                                    <EventTrigger RoutedEvent="Loaded">
                                        <BeginStoryboard Storyboard="{StaticResource FadeInStoryboard}"/>
                                    </EventTrigger>
                                </Border.Triggers>
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="BorderBrush" Value="#CCCCCC"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Effect">
                                                    <Setter.Value>
                                                        <DropShadowEffect BlurRadius="10" ShadowDepth="2" Color="#333" Opacity="0.3"/>
                                                    </Setter.Value>
                                                </Setter>
                                                <Setter Property="RenderTransform">
                                                    <Setter.Value>
                                                        <TranslateTransform Y="-3"/>
                                                    </Setter.Value>
                                                </Setter>
                                            </Trigger>
                                            <DataTrigger Binding="{Binding AverageGrade, Mode=OneWay}" Value="2">
                                                <Setter Property="Background" Value="#FFEEEE"/>
                                                <Setter Property="BorderBrush" Value="#FF0000"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding AverageGrade, Mode=OneWay}" Value="2.5">
                                                <Setter Property="Background" Value="#FFEEEE"/>
                                                <Setter Property="BorderBrush" Value="#FF0000"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding AverageGrade, Mode=OneWay}" Value="3">
                                                <Setter Property="Background" Value="#FFF5E6"/>
                                                <Setter Property="BorderBrush" Value="#FFA500"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="250"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="200"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" Margin="0,0,20,0">
                                        <TextBlock Text="{Binding FullName}" FontWeight="Bold" FontSize="16" Margin="0,0,0,10" TextWrapping="Wrap"/>
                                        <TextBlock Text="Средний балл:" FontSize="12" Margin="0,0,0,5" Foreground="#666"/>
                                        <TextBlock Text="{Binding AverageGrade, Mode=OneWay, StringFormat={}{0:F2}}" 
                                                   FontSize="24" FontWeight="Bold" Foreground="Green">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding AverageGrade, Mode=OneWay}" Value="2">
                                                            <Setter Property="Foreground" Value="Red"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding AverageGrade, Mode=OneWay}" Value="2.5">
                                                            <Setter Property="Foreground" Value="Red"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding AverageGrade, Mode=OneWay}" Value="3">
                                                            <Setter Property="Foreground" Value="Orange"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <ProgressBar Value="{Binding AverageGrade, Mode=OneWay}" Maximum="5" Height="8" Margin="0,10,0,0" Background="#E0E0E0"/>
                                        <TextBlock Text="← Кликните для выбора" FontSize="11" Foreground="#999" Margin="0,5,0,0" FontStyle="Italic"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" Margin="0,0,20,0">
                                        <TextBlock Text="Оценки:" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
                                        <ItemsControl ItemsSource="{Binding Grades}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal" Margin="0,3">
                                                        <TextBlock Text="{Binding Subject}" Width="150" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                                        <Border Background="#2196F3" CornerRadius="4" Padding="8,2" Margin="5,0">
                                                            <TextBlock Text="{Binding Value}" Foreground="White" FontWeight="Bold"/>
                                                        </Border>
                                                        <TextBox Text="{Binding Comment}" Width="250" Margin="10,0" Background="Transparent" BorderThickness="0,0,0,1"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Посещаемость:" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
                                        <ItemsControl ItemsSource="{Binding Attendance}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal" Margin="0,3">
                                                        <TextBlock Text="{Binding Date, StringFormat=dd.MM.yyyy}" Width="80"/>
                                                        <CheckBox IsChecked="{Binding IsPresent}" Margin="5,0"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>