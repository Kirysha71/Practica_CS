<Window x:Class="GradeJournal.ChatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Чат" Height="500" Width="400"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Ellipse Width="10" Height="10" Margin="0,0,5,0" Fill="{Binding ConnectionColor}"/>
            <TextBlock Text="{Binding ConnectionStatusText}" FontWeight="Bold" VerticalAlignment="Center"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" x:Name="MessagesScroll" VerticalScrollBarVisibility="Auto" 
                      Margin="0,0,0,10" Background="#F5F5F5" Padding="5">
            <ItemsControl ItemsSource="{Binding Messages}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="5" Padding="8" CornerRadius="5">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="#F5F5F5"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsFromCurrentUser}" Value="True">
                                            <Setter Property="Background" Value="#E3F2FD"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <StackPanel>
                                <TextBlock Text="{Binding Sender}" FontWeight="Bold" FontSize="11" 
                                           Foreground="#666" Margin="0,0,0,2"/>
                                <TextBlock Text="{Binding Text}" TextWrapping="Wrap" FontSize="13"/>
                                <TextBlock Text="{Binding Time, StringFormat=HH:mm}" FontSize="10" 
                                           Foreground="#999" HorizontalAlignment="Right" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" Text="{Binding MessageText, UpdateSourceTrigger=PropertyChanged}" 
                     Height="40" Padding="10" FontSize="14"
                     AcceptsReturn="True" TextWrapping="Wrap"
                     KeyDown="TextBox_KeyDown"/>

            <Button Grid.Column="1" Content="➤" Width="50" Margin="5,0,0,0"
                    Command="{Binding SendMessageCommand}"
                    FontSize="16" FontWeight="Bold"/>
        </Grid>
    </Grid>
</Window>